# AI 提升路线图（ROADMAP · 体验/功能/质量 全维度）

> 触发：动效与加固收尾后，用户要求「分析还有哪里可以提升」。
> 性质：AI 可直接消费的下一阶段清单。每项含【价值 / 成本 / 位置 / 改法 / 验收】，按主题契合度与性价比排序。
> 基线：commit ed79aed + MOTION v3（读条式开机）。30/30 tests。
> 方法：全代码走查 + WCAG 对比度实测 + 功能覆盖盘点。

---

## 〇、现状快评

| 维度 | 评分 | 一句话 |
|---|---|---|
| 动效 | ★★★★☆ | v3 读条开机 + 跟手修复后已达良好，剩 P2 打磨项 |
| 视觉主题 | ★★★★☆ | CRT 磷光绿完整，但**单主题**、特效强度不可调 |
| 交互 | ★★★☆☆ | 快捷键存在但**无提示**、卡片不可整点、无键盘流导航 |
| 功能 | ★★★☆☆ | 核心闭环完整，缺过滤/排序/URL 状态 |
| 健壮性 | ★★★★☆ | FIXLIST 后加固到位，缺离线检测 |
| 可访问性 | ★★★☆☆ | tabs/RM/aria 已做；**faint 对比度不达标（实测 3.06:1）** |

---

## 一、实测数据支撑的两个实锤

1. **`--faint`（#1f6b40）对比度 3.06:1 < WCAG AA 4.5:1**，且用于 hint-line（12px 快捷键/帮助文案——信息密度最高的提示）、cmd-echo 前缀、loading-sub、empty-line。小字 + 低对比 = 可读性最差组合。
   全站对比度实测：ink 11.57 ✓ / bright 15.89 ✓ / dim 6.00 ✓ / amber 11.26 ✓ / red 6.54 ✓ / **faint 3.06 ✗**。
2. **快捷键是隐藏功能**：1/2/3/R 已实现，hint 行只写「点仓库名直达 · [COPY] 复制链接」——新用户永远发现不了。

---

## 二、提升项清单

### P0 · 主题契合的杀手级（✅ 已全部实施，见各项标注）

**R-01 · vim 式键盘导航 ✅** ★★★★★ / 成本中
- 位置：app.js（keydown 处理器扩展 + `cursor` 高亮类）
- 改法：`j`/`k`（或 ↓/↑）在 .repo 间移动高亮（`.repo.cursor-line`：左边框亮 + 微辉光），`Enter` 打开当前项链接，`c` 复制当前项；Esc 取消。与 1/2/3/R 并存。
- 为什么是 P0：**终端主题的灵魂交互**——鼠标流之外补全键盘流，气质完全统一。
- 验收：j/k 移动时高亮跟随滚动进视口（scrollIntoView block:'nearest'）；RM 不受影响。

**R-02 · 琥珀单色主题切换 [THEME] ⛔ 已实施后按用户要求整体移除（2026-08）——色板与切换器代码不再存在；若重启需重写并重新过对比度哨兵** ★★★★☆ / 成本低
- 位置：style.css（:root 变量已全部 token 化，天然支持）；cmdbar 加按钮
- 改法：`body.theme-amber { --ink:#ffb454; --bright:#ffd07a; --dim:#c88a3a; --faint:#8a5c28; --line:#5c3d1a; --glow:琥珀辉光; ... }`；localStorage 存偏好；按钮循环 green→amber。**新主题全色阶需过 4.5:1 实测**（faint 教训）。
- 验收：两主题下全文本色对比度脚本断言 ≥4.5。

**R-03 · URL 状态 `?since=daily` ✅（附带修复 serveStatic 带 query 根路径 404）** ★★★★ / 成本低
- 位置：app.js boot（读取 URLSearchParams 优先于 localStorage）+ selectPeriod（history.replaceState 同步）
- 验收：带参打开直达榜单；切榜后 URL 变化；刷新保持。

**R-04 · faint 对比度修复 ✅（#2f9558, 5.25:1）** ★★★★ / 成本极低
- 改法：`--faint: #2a8a52`（预估 ~4.6:1，落地后实测确认 ≥4.5）；hint-line 字号 12→12.5px 可选。
- 验收：对比度脚本进 test（源码哨兵：解析 :root 十六进制断言）。

### P1 · 体验完善（第二批）

**R-05 · FX 强度开关 [FX]** ★★★★ / 成本低
- 改法：body.fx-off 关闭扫描线闪烁（body::after animation）与 crt-flicker；三档循环 高→低→关，localStorage 记忆。光敏/晕动友好（与 RM 互补：RM 是"全不要"，FX 是"只要静态质感"）。
- 验收：fx-off 下 body::after 无 animation。

**R-06 · 整卡可点** ★★★ / 成本低
- 改法：.repo 加 click → window.open(url)（排除 .mini 按钮冒泡）；cursor:pointer。
- 验收：点卡片空白处打开仓库；COPY 按钮不触发。

**R-07 · 离线检测** ★★★★ / 成本低
- 改法：navigator.onLine + online/offline 事件 → 状态栏 `$ OFFLINE · 显示缓存`（status-offline 类已有）。
- 验收：DevTools 离线模拟 → 状态栏即时切换。

**R-08 · 快捷键帮助补全 ✅（修正：1/2/3/R 原有提示，本次补 J/K/ENTER/C）** ★★★ / 成本极低
- 改法：hint 行追加 `[1/2/3] 切换榜单 · [R] 同步 · [J/K] 导航 · [Enter] 打开`；配合 R-04 提亮的 faint。
- 验收：hint 行文案包含全部可用键位。

**R-09 · 相对时间自动走动** ★★★ / 成本低
- 改法：setInterval 60s 重渲染 sync-head 的 humanAge 部分（仅文本节点更新，不动卡片）。

### P2 · 按需（第三批）

**R-10 · 真命令行输入** ★★★★★ / 成本高：cmdbar 变真输入行，`daily`/`theme amber`/`sort stars`/`help`——终端主题终极形态，值得单独立项。
**R-11 · Service Worker 离线** ★★★★ / 成本中：静态资源 SW 预缓存，断网刷新可用（README「离线兜底」卖点的完全体）。注意 ?v= 指纹与 SW 缓存键的配合。
**R-12 · 排序切换** ★★★ / 成本低：`sort stars|growth`，纯前端重排 + 瀑布重放。
**R-13 · README 新截图** ★★ / 成本极低：读条开机后的新 UI 截图（work/ui-shot.mjs 现成）。
**R-14 · 长描述截断/展开** ★★ / 成本低：desc 3 行截断，点击展开。

---

## 三、推荐路线

1. **第一批（P0）**：R-04 → R-08 → R-03 → R-02 → R-01（先修 a11y 与提示，再上键盘流与新主题）
2. **第二批（P1）**：R-05 / R-06 / R-07 / R-09
3. **第三批（P2 按需）**：R-10 命令行单独立项；R-11 SW；其余顺手
4. 每批：node --test 30 条基线 + ui-shot 截图 + 新增源码哨兵/CDP 断言

## 四、明确不做

- ❌ 深色/浅色双主题——CRT 只有单色荧光，浅色违背主题
- ❌ 鼠标悬停预览卡片大图——数据源无图，别造假
- ❌ 自动刷新轮询——用户已有 R 键与后台刷新，轮询徒增上游压力（FIXLIST ISSUE-05 教训）